<!DOCTYPE html>
<html>
  <head>
    <title>Serverless Weather - Success</title>
    <link
      href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="flex items-center content-center h-screen">
    <div class="max-w-lg mx-auto p-4 bg-black shadow-xl text-center w-full">
      <div class="bg-white p-10">
        <h1 class="font-bold text-4xl">
          <%= weather.location.name %>
        </h1>
        <span class="font-light italic">
            <%= weather.location.country %>
        </span>
        <h2 class="font-hairline py-4 text-6xl">
          <span>
            <%= weather.current.temperature %>
          </span>
          <span>
            &deg;C
          </span>
        </h2>
        <span class="font-light font-mono text-gray-700">
          <%= weather.current.weather_descriptions[0] %>
        </span>
      </div>
    </div>
  </body>
</html>
